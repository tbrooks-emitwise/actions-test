name: MultiRequired
on:
  pull_request:
    paths-ignore:
      - "path1/**"
      - "path2/**"
jobs:
  build:
    name: Multi real build
    runs-on: ubuntu-latest
    steps:
      - name: Do build
        run: 'echo "Nothing to do"'

